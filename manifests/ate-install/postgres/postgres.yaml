# Copyright 2026 Google LLC
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-config
  namespace: ate-system
data:
  postgresql.conf: |
    listen_addresses = '*'
    ssl = on
    ssl_cert_file = '/run/servicedns.podcert.ate.dev/credential-bundle.pem'
    ssl_key_file = '/run/servicedns.podcert.ate.dev/credential-bundle.pem'
    ssl_ca_file = '/run/podidentity.podcert.ate.dev/trust-bundle.pem'
    hba_file = '/etc/postgresql/pg_hba.conf'
  pg_hba.conf: |
    # Local socket access is limited to processes in this pod and is used by
    # health checks, the workload's idempotent database bootstrap, and the
    # tls-reloader sidecar's configuration reloads.
    local all all trust
    # Only ateapi's two roles may connect over TCP. PostgreSQL requires both a
    # password and a client certificate signed by the pod-identity CA.
    hostssl atepg ateapi_runtime all scram-sha-256 clientcert=verify-ca
    hostssl atepg ateapi_ddl all scram-sha-256 clientcert=verify-ca
  reload-tls.sh: |
    # PostgreSQL opens ssl_cert_file, ssl_key_file and ssl_ca_file at startup
    # and on SIGHUP, and nowhere else. The kubelet replaces the projected pod
    # certificate in place about 30 minutes before it expires, so without this
    # loop the server keeps presenting the certificate it booted with until it
    # expires about a day later and every client stops trusting it.
    set -eu

    # As PID 1 this shell only sees SIGTERM if a handler is installed, and only
    # acts on it between commands, so the sleep below runs in the background
    # and is waited on. Without both halves the pod takes the full termination
    # grace period to go away.
    trap 'exit 0' TERM INT

    CERT=/run/servicedns.podcert.ate.dev/credential-bundle.pem
    CA=/run/podidentity.podcert.ate.dev/trust-bundle.pem

    # Comfortably inside the 30m headroom (notAfter - beginRefreshAt) that
    # cmd/podcertcontroller/internal/servicednssigner/servicednssigner.go
    # leaves; hashing two small files costs nothing.
    INTERVAL=60

    reloaded=""
    while true; do
      current="$(sha256sum "${CERT}" "${CA}" /etc/postgresql/pg_hba.conf)"
      # Reloading fails until the server is accepting connections, which is
      # where every pod starts out, so only record a hash once it has worked.
      # Starting empty also means a restart of this container costs one
      # redundant reload rather than a missed one.
      if [ "${current}" != "${reloaded}" ] \
        && psql -U postgres -d postgres -Atc 'SELECT pg_reload_conf()' >/dev/null 2>&1; then
        reloaded="${current}"
        echo "$(date -u +%FT%TZ) reloaded TLS configuration"
      fi
      sleep "${INTERVAL}" &
      wait $!
    done
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: ate-system
spec:
  clusterIP: None
  selector:
    app: postgres
  ports:
  - name: postgres
    port: 5432
    targetPort: 5432
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres
  namespace: ate-system
spec:
  serviceName: postgres
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      securityContext:
        # Group ownership of the projected certificate below, and of the data
        # volume so that a freshly provisioned one is writable. OnRootMismatch
        # keeps the kubelet from walking the data directory on every start,
        # which would leave PGDATA group-writable and postgres refusing to run.
        fsGroup: 70
        fsGroupChangePolicy: OnRootMismatch
      # PostgreSQL re-reads its TLS files only on SIGHUP, so this sidecar
      # reloads the server whenever the kubelet rotates the projected pod
      # certificate. fsGroup is also what makes that projection readable: the
      # kubelet writes it root-owned for as long as the pod's containers do not
      # all agree on one non-root user, and grants the fsGroup group access,
      # landing the key at root:postgres 0640, the only shared mode PostgreSQL
      # accepts. Pinning runAsUser on the postgres container would make the key
      # postgres-owned and group-readable, which it rejects.
      # See https://www.postgresql.org/docs/current/ssl-tcp.html#SSL-SETUP
      initContainers:
      - name: tls-reloader
        restartPolicy: Always
        image: postgres:18-alpine@sha256:9a8afca54e7861fd90fab5fdf4c42477a6b1cb7d293595148e674e0a3181de15
        securityContext:
          runAsUser: 70
        command:
        - /bin/sh
        - /etc/postgresql/reload-tls.sh
        volumeMounts:
        - name: config
          mountPath: /etc/postgresql
        - name: servicedns
          mountPath: /run/servicedns.podcert.ate.dev
          readOnly: true
        - name: podidentity-ca
          mountPath: /run/podidentity.podcert.ate.dev
          readOnly: true
        - name: socket
          mountPath: /var/run/postgresql
        resources:
          requests:
            cpu: 10m
            memory: 32Mi
      containers:
      - name: postgres
        image: postgres:18-alpine@sha256:9a8afca54e7861fd90fab5fdf4c42477a6b1cb7d293595148e674e0a3181de15
        lifecycle:
          postStart:
            exec:
              command:
              - /bin/sh
              - -ec
              - |
                until psql -U postgres -d postgres -Atc 'SELECT 1' >/dev/null 2>&1; do
                  sleep 1
                done
                if ! psql -U postgres -d postgres -Atc \
                  "SELECT 1 FROM pg_database WHERE datname = 'atepg'" | grep -qx 1; then
                  createdb -U postgres atepg
                fi
                psql -v ON_ERROR_STOP=1 -U postgres -d postgres \
                  -v runtime_password="${ATEAPI_RUNTIME_PASSWORD}" \
                  -v ddl_password="${ATEAPI_DDL_PASSWORD}" <<'SQL'
                SET password_encryption = 'scram-sha-256';
                SELECT 'CREATE ROLE ateapi_runtime LOGIN'
                  WHERE NOT EXISTS (SELECT FROM pg_roles WHERE rolname = 'ateapi_runtime') \gexec
                SELECT 'CREATE ROLE ateapi_ddl LOGIN'
                  WHERE NOT EXISTS (SELECT FROM pg_roles WHERE rolname = 'ateapi_ddl') \gexec
                ALTER ROLE ateapi_runtime NOSUPERUSER NOCREATEDB NOCREATEROLE NOREPLICATION PASSWORD :'runtime_password';
                ALTER ROLE ateapi_ddl NOSUPERUSER NOCREATEDB NOCREATEROLE NOREPLICATION PASSWORD :'ddl_password';
                REVOKE CONNECT, CREATE, TEMPORARY ON DATABASE atepg FROM PUBLIC;
                GRANT CONNECT ON DATABASE atepg TO ateapi_runtime, ateapi_ddl;
                GRANT CREATE ON DATABASE atepg TO ateapi_ddl;
                SQL
                psql -v ON_ERROR_STOP=1 -U postgres -d atepg \
                  -v substrate_schema="${ATEAPI_POSTGRES_SCHEMA}" <<'SQL'
                SELECT format('REVOKE ALL ON SCHEMA %I FROM PUBLIC', :'substrate_schema')
                  WHERE EXISTS (SELECT FROM pg_namespace WHERE nspname = :'substrate_schema') \gexec
                SELECT format('ALTER SCHEMA %I OWNER TO ateapi_ddl', :'substrate_schema')
                  WHERE EXISTS (SELECT FROM pg_namespace WHERE nspname = :'substrate_schema') \gexec
                SELECT format('ALTER TABLE %I.%I OWNER TO ateapi_ddl', n.nspname, c.relname)
                  FROM pg_class c
                  JOIN pg_namespace n ON n.oid = c.relnamespace
                  WHERE n.nspname = :'substrate_schema'
                    AND c.relkind IN ('r', 'p')
                    AND c.relowner <> (SELECT oid FROM pg_roles WHERE rolname = 'ateapi_ddl')
                  \gexec
                SELECT format('ALTER SEQUENCE %I.%I OWNER TO ateapi_ddl', n.nspname, c.relname)
                  FROM pg_class c
                  JOIN pg_namespace n ON n.oid = c.relnamespace
                  WHERE n.nspname = :'substrate_schema'
                    AND c.relkind = 'S'
                    AND c.relowner <> (SELECT oid FROM pg_roles WHERE rolname = 'ateapi_ddl')
                  \gexec
                SQL
        env:
        - name: POSTGRES_DB
          value: atepg
        - name: POSTGRES_HOST_AUTH_METHOD
          value: trust
        - name: PGDATA
          value: /var/lib/postgresql/data/pgdata
        - name: ATEAPI_RUNTIME_PASSWORD
          valueFrom:
            secretKeyRef:
              name: postgres-role-passwords
              key: runtime-password
        - name: ATEAPI_DDL_PASSWORD
          valueFrom:
            secretKeyRef:
              name: postgres-role-passwords
              key: ddl-password
        - name: ATEAPI_POSTGRES_SCHEMA
          valueFrom:
            configMapKeyRef:
              name: ate-api-server-envvars
              key: ATE_API_POSTGRES_SCHEMA
        ports:
        - name: postgres
          containerPort: 5432
        readinessProbe:
          exec:
            command:
            - /bin/sh
            - -ec
            - psql -U postgres -d atepg -Atc 'SELECT 1' >/dev/null
          initialDelaySeconds: 2
          periodSeconds: 2
        livenessProbe:
          exec:
            command:
            - pg_isready
            - -U
            - postgres
            - -d
            - postgres
          initialDelaySeconds: 10
          periodSeconds: 10
        args:
        - -c
        - config_file=/etc/postgresql/postgresql.conf
        volumeMounts:
        - name: config
          mountPath: /etc/postgresql
        - name: servicedns
          mountPath: /run/servicedns.podcert.ate.dev
          readOnly: true
        - name: podidentity-ca
          mountPath: /run/podidentity.podcert.ate.dev
          readOnly: true
        - name: socket
          mountPath: /var/run/postgresql
        - name: data
          mountPath: /var/lib/postgresql/data
        # The whole control plane shares this one database; under activation
        # load it sustains several cores. The kind overlay shrinks this back
        # down to fit a CI runner.
        resources:
          requests:
            cpu: "2"
            memory: "1Gi"
          limits:
            cpu: "16"
            memory: "2Gi"
      volumes:
      - name: config
        configMap:
          name: postgres-config
      - name: servicedns
        projected:
          # 0600 plus the group read that fsGroup adds is the 0640 above.
          defaultMode: 0600
          sources:
          - podCertificate:
              signerName: servicedns.podcert.ate.dev/identity
              keyType: ECDSAP256
              credentialBundlePath: credential-bundle.pem
      # The unix socket directory, shared so the sidecar can ask the running
      # server to reload. The image defaults both the server and its clients to
      # this path, so nothing else has to know about it.
      - name: socket
        emptyDir: {}
      - name: podidentity-ca
        projected:
          sources:
          - clusterTrustBundle:
              signerName: podidentity.podcert.ate.dev/identity
              labelSelector:
                matchLabels:
                  podcert.ate.dev/canarying: live
              path: trust-bundle.pem
  volumeClaimTemplates:
  - metadata:
      name: data
    spec:
      accessModes: [ "ReadWriteOnce" ]
      resources:
        requests:
          # Headroom so WAL churn cannot fill the volume: a full pg_wal is a
          # PANIC that drops every connection. Size also scales provisioned
          # IOPS/throughput on PD-backed storage classes. The kind overlay
          # shrinks this claim back down.
          storage: 500Gi
